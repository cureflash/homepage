# Core

Native domain engines live here: QuizSession, WorkoutBuilder, MasteryEngine, ReviewScheduler and ProgressionEngine. Implement them from the frozen Web contracts and shared conformance fixture.
