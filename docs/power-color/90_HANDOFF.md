# Power Color — HANDOFF

## Active work
Grade 1 is active by explicit user override. Grade 2 remains 348 verified / pending 0 and incomplete at its source-blocked frontier. Grade 3 remains 223 verified / pending 0 and incomplete with its recorded blockers. No lower-grade source/display gate is relaxed.

Canonical Grade 1 working set: `STATUS.json`, `20_EXECUTION_PLAN.md`, `60_GRADE1_EXECUTION_PLAN.md`, `61_GRADE1_SOURCE_INVENTORY.md`, `62_GRADE1_SPEC.md`.

## Grade 1 frontier
All current official Grade 1 textbook TOC content items through p.160 have been source-audited in order. Earlier detailed decisions remain in `61_GRADE1_SOURCE_INVENTORY.md` and individual audit files.

p.152 `資料・光と色（1） — 色の知覚` is `SOURCE_RESOLVED` to the current AFT official first-stage sample slice; 4/4 verified. p.157 `資料・光と色（2） — 相関色温度と演色評価数` is `SOURCE_RESOLVED` to narrow current CIE/JLMA definitions; 4/4 verified. p.160 `資料・CMF® — 素材と工法` remains `SOURCE_BLOCKED`; generic CMF/materials-engineering taxonomies remain rejected as substitutes.

The canonical Grade 1 runtime/promotion path uses the existing shared Power TOEIC question-bank/workout/session engine. p.006, p.007, p.010, p.014, p.017, p.021 culture batches plus p.024 Judd, p.025 Chevreul, p.027 Rood, p.028 Ostwald, p.029 Itten, p.030 Moon & Spencer, p.032 color appearance, p.036 lighting properties, p.039 measurement illuminants, p.041 Grassmann's laws, p.046 XYZ, p.050 CIELAB, p.053 Ostwald hue circle, p.054 Ostwald color notation, and p.054 Ostwald equal-hue plane are now promoted record-identically. The promotion gate enforces runtime↔authoring deep equality, full-fingerprint uniqueness, answer-position auditing and shared-engine execution. Latest Power Color CI `33848895318` succeeded and PR #782 merged as `6294161a854c62fe1713633d7730285dae4ab686`. Runtime is 105 verified / pending 0 across twenty-one skills; A/B/C/D=27/27/27/24; `productionApproved` remains false.

## Source/display boundary
Do not reconstruct unpublished AFT wording, diagrams, formulas, examples, thresholds, geometry, RGB/HEX/display values, monitor-facing judgments, exact business/CMF/fashion process sequences, fiber-length color-development rules, VMD initiative/color procedures, AFT-specific near/mid/far color-planning prescriptions, or the p.160 material/process set. Do not repeat recorded rejected source routes without new explicit evidence.

p.035 `色の分類` remains `SOURCE_BLOCKED`; do not reconstruct its exact AFT taxonomy, labels, counts, examples, or diagrams. Skip it in the runtime promotion sequence until new explicit auditable authority appears.

## Exact next start point
Promote the existing p.055 `pc1.color_representation.ostwald_color_solid_p055` batch 3/3 verified records record-identically through the canonical Grade 1 runtime gate. Source contract: `docs/power-color/audits/2026-09-04-grade1-p055-ostwald-color-solid-source-audit.md`. Authoring: `qualifications/color-certification/data/grade1-authoring-ostwald-color-solid-p055-0001-0003.json`. Classification remains `SECONDARY_CORROBORATION_REQUIRED`; the accepted narrow nonvisual boundary is limited to the three audited fact families already encoded in that verified batch: the color solid is made with Ostwald hue triangles; important clear sequences lie on the outside surface; equal-blackness and equal-whiteness color scales are defined. Do not reconstruct unpublished AFT geometry, orientation, dimensions, cross-sections, notation, examples, RGB/HEX/display values, or visual judgments. Continue only verified authoring in official TOC order and keep `productionApproved=false`.

Operational write-path audit (2026-09-04): the connected GitHub surface can retrieve the complete existing `grade1-runtime.json` through the blob endpoint; the previous concern that only truncated runtime content could be read is no longer accurate. The remaining limitation is narrower: the available write actions still require supplying a complete replacement blob and do not provide a server-side partial patch/compose operation. Do not manually reconstruct that large JSON payload or introduce a fragment loader, alternate runtime format, monkeypatch, or gate bypass. Exact resume point remains the same three canonical p.055 records; promote them only through a path that preserves every existing runtime record and appends those three canonical records record-identically.

Git-data verification (2026-09-04): `create_tree` / `create_commit` do not remove this blocker. A tree can preserve every unrelated path and can point `grade1-runtime.json` at a replacement blob, but Git blobs are atomic and this surface still has no operation that composes the existing runtime blob with the three canonical p.055 records server-side. Therefore creating a tree still requires first supplying the complete new runtime blob. This route was rejected because manually reconstructing or reserializing the 137392-byte runtime through the connector would weaken the record-preservation guarantee. Resume only in an execution environment that can check out/materialize the existing file bytes and apply a normal local diff, or if a future connector exposes safe partial-file patch/blob composition.
