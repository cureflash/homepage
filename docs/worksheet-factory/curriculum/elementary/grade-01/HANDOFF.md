# 小学1年 算数 — HANDOFF

更新: 2026-08-25

## 今回完了

- 小1の追加seed公開を機械化する `scripts/publish_grade1_variants.py` を追加。
- 既存 `worksheet_factory.py` の `SKILLS`・決定的seed生成・独立解答検証・正規化ハッシュ・PDFレンダラー・カタログ検証をそのまま利用し、別実装の問題生成を増やさない構成にした。
- 未登録IDだけを追加し、既存 `content_hash` と衝突した場合は公開前に停止する。
- seed=202/303 を variant 02/03 として全15技能へ一括展開できる。
- 出力PDFを生成後に存在・サイズを確認し、その後にカタログ全体を `validate_catalog` で検証してから保存する。
- `tests/test_grade1_variant_publisher.py` を追加し、全15技能×2seedについて決定性・独立解答検証・ハッシュ重複なし・メタデータを検査する。
- 一時リポジトリで seed=202 の15PDF生成とカタログ追記まで通す回帰テストも追加した。

## 既完了の内容技能

- 小1の15技能すべてについて20問版 seed=101 を公開済み。
- 各技能は seed=101/202/303 で決定性・独立解答再計算・値域を検証済み。
- 問題番号は整数表示、解答ページは同一問題配置＋赤字解答の共通形式。

## 未完了

- 追加seed版PDFを実際に生成・登録して公開する。
- 50問版。
- speed版。
- 50問版に必要な複数問題ページ対応。

## 次にやること

`python scripts/publish_grade1_variants.py . --seeds 202 303` 相当のバッチを安全な実行環境で走らせ、生成された30PDFと更新済み `worksheets/catalog.json` を検証・公開する。その後、50問版向けにPDFレンダラーを複数問題ページ対応へ変更する。
