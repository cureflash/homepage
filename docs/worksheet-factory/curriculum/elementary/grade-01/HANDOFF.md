# 小学1年 算数 — HANDOFF

更新: 2026-08-25

## 今回完了

- 小1の未完了だった4技能を生成器へ追加。
  - 2桁の数の大小比較
  - 数の並び・前後の数
  - 簡単な2桁 + 1桁 / 2桁 - 1桁
  - 小学1年 算数 総復習
- 大小比較は `<` `>` `=` を独立再計算し、カタログの `answer_type` を `accepted-set` に設定。
- 前後の数は11〜98を中心値にし、答えが10〜99に収まるよう実装。
- 2桁±1桁は繰り上がり・繰り下がりのない簡単な計算として実装。
- 総復習は小1で実装済みの数値回答形式を混合する `review` 系列として実装。
- 新4技能を seed=101/202/303 で決定性・独立解答再計算・値域・正規化ハッシュ重複なしを検証。
- seed=101版4PDFを公開し、`worksheets/catalog.json` に登録。
- 公開カタログは小1の15技能・15プリントになった。
- 新4PDFは共通レイアウト規則どおり、整数番号と問題式を分離し、2ページ目は同一問題配置に赤字で解答を追記。
- 新4技能用の回帰テスト `tests/test_grade1_remaining_skills.py` を追加。

## 検証

- 新4技能は seed=101/202/303 で同一seed再生成が一致。
- 全問題で独立再計算結果と生成答えが一致。
- seed違いの正規化ハッシュは技能ごとに重複なし。
- 大小比較は両数10〜99、答えは `<` `>` `=`。
- 前後の数は中心11〜98、答え10〜99。
- 2桁±1桁は加法で繰り上がりなし、減法で繰り下がりなし。
- 新4PDFはいずれも2ページで、GitHub上の公開パスとカタログURLが一致。
- 新4PDFを画像レンダリングし、番号・赤字解答・重なり・欠けを確認。

## 未完了

- 50問版
- speed版
- 既存技能を含む追加seed公開版

## 次にやること

小1の15技能について追加seed公開版を作り、あわせて50問版・speed版の生成仕様を実装する。複数variant公開まで完了したら小1を `done` にする。
