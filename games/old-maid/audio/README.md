# Audio files

Place optional audio files here with these names:

- `bgm.mp3` — looping BGM
- `select.mp3` — card/mode selection SE
- `correct.mp3` — correct pair SE
- `wrong.mp3` — game-over SE
- `clear.mp3` — clear SE

The game continues to work if these files are absent.
